'use strict'
angular.module('app.clients', [])
# --------------------------------------------
# Contact in Handbook TAB of Client
#
.controller('ContactCtrl', [
    '$scope', '$filter' , 'fetchTabData', 'fakeData', '$location', '$routeParams'
    ($scope, $filter, fetchTabData, fakeData, $location, $routeParams) ->

])
